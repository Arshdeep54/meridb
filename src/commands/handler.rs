pub fn handle_select(command: &str) {
    println!("Handling SELECT command: {}", command);
}

pub fn handle_insert(command: &str) {
    println!("Handling INSERT command: {}", command);
}

pub fn handle_update(command: &str) {
    println!("Handling UPDATE command: {}", command);
}

pub fn handle_delete(command: &str) {
    println!("Handling DELETE command: {}", command);
}