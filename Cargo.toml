[package]
name = "meridb"
version = "0.1.0"
edition = "2021"

[dependencies]
clap = { version = "4.0", features = ["derive"] }
anyhow = { version = "1.0", default-features = false }
termios = "0.3"
libc = "0.2"